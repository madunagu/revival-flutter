import 'package:flutter/material.dart';

class BottomSheetLine extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      width: 47,
      height: 6,
      decoration: BoxDecoration(
        borderRadius: BorderRadius.circular(3),
        color: Color(0x669599B3),
      ),
    );
  }
}
